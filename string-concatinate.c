#include<stdio.h>
#include<string.h>
int main(){
    char first-name[30],last-name[30];
    printf("Enter name: ");
    gets(first-name);
    gets(last-name);
    strcat(first-name,last-name);
    printf("the name is (paste as):\t");
    puts(paste);
    return 0;
}
